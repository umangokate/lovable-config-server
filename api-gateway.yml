server:
  port: 8080

spring:
  cloud:
    gatewat.server.webflux:
      routes:
        - id: account-service
          uri: lb://account-service
          predicates:
            - Path=/api/v1/account/**
          filters:
            - StripPrefix=2
